# RAG Backend

Production-style RAG (Retrieval-Augmented Generation) backend.

## Structure
- `app/`: Source code
- `data/`: PDF storage

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run server: `uvicorn app.main:app --reload`
